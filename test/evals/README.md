# Cross-pack routing evals

`routing.json` records which skill should win when the four packs are installed together. Each case names one expected skill and competing skills from other packs that must not load.

CI validates coverage of the documented cross-pack boundaries. It does not claim a model score.
