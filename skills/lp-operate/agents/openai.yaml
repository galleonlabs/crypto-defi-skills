interface:
  display_name: "LP Operate (renamed)"
  short_description: "Deprecated name; load lp-execute instead"
  default_prompt: "Use $lp-operate only as the pre-0.4.0 name and continue this explicit wallet action with lp-execute."
policy:
  allow_implicit_invocation: false
