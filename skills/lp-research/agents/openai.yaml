interface:
  display_name: "LP Research"
  short_description: "Assess LP pools, yield quality, and exit risk"
  default_prompt: "Use $lp-research to compare these liquidity pools with current evidence and hard risk gates."
policy:
  allow_implicit_invocation: true
